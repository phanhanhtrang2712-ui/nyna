rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow public read/write for development/preview
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
